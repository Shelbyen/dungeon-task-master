# Generated by Django 5.2.8 on 2026-01-10 04:04

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('tasks', '0007_fix_foreign_key_with_raw_sql'),
    ]

    operations = [
        migrations.RunSQL(
            # Enable foreign key constraints and verify the table structure
            """
            PRAGMA foreign_keys = OFF;

            -- Recreate the table with proper foreign key constraint
            CREATE TABLE tasks_task_new (
                id integer NOT NULL PRIMARY KEY AUTOINCREMENT,
                name varchar(255) NOT NULL,
                description text,
                status varchar(20) NOT NULL,
                created_at datetime NOT NULL,
                due_date datetime NOT NULL,
                priority varchar(10) NOT NULL,
                updated_at datetime NOT NULL,
                user_id integer NOT NULL REFERENCES users_user(id)
            );

            -- Copy any existing data
            INSERT INTO tasks_task_new SELECT * FROM tasks_task;

            -- Drop old table and rename new one
            DROP TABLE tasks_task;
            ALTER TABLE tasks_task_new RENAME TO tasks_task;

            -- Create index
            CREATE INDEX tasks_task_user_id_f0e531b0 ON tasks_task (user_id);

            PRAGMA foreign_keys = ON;
            """,
            reverse_sql="SELECT 1;"
        ),
    ]
